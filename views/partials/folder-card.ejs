<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
    <title>Folder Card</title>
</head>

<body>
    <% if(cardData){ %>
        <% console.log(cardData) %>
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">Nume folder: <%= cardData.name %>
                    </h5>
                    <p class="card-text">Card Text (aici nu stiu ce pot sa pun - poate o descriere)</p>
                    <a href="/files/folder/<%=cardData.id%>" class="btn btn-primary">Open Folder</a>
                </div>
            </div>
            <% } %>

</body>

</html>