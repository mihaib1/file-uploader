<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
    <title>User Page</title>
</head>

<body>
    <div class="container mt-5">
        <div class="header">
            <div class="row">
                <% if(user) { %>
                    <div class="col">
                        <h1>Hello, <%= user.fullName %>!</h1>
                    </div>
                    <div class="col">
                        <a href="files/create-folder"><button class="btn btn-success">Create a folder</button></a>
                        <a href="files/view-folders"><button class="btn btn-success">View Folders</button></a>
                        <a href="/files/upload"><button class="btn btn-success">Add file</button></a>
                        <form action="/logout?_method=DELETE" method="POST" class="mt-4">
                            <button type="submit" class="btn btn-danger">Log Out</button></a>
                        </form>
                    </div>


                    <% } else { %>
                        <h1>Hello, user! You are not logged in.</h1>
                        <% } %>
            </div>

        </div>


    </div>

</body>

</html>